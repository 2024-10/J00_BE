<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/base.css">
  <link rel="stylesheet" href="/css/createMandalart.css">
</head>
<body>
  <header id="main-header">
    <a href="/">
        <img src="/images/logo.svg" alt="logo" class="logo">
    </a>
    <nav id="main-navigation">
        <ul class="menu">
          <% if (user) { %>
            <!-- 로그인 상태일 때, 로그아웃 버튼 -->
            <li class="menu-item">
                <form id="logout-form" action="/logout" method="POST">
                    <button type="submit" class="signup-link">로그아웃</button>
                </form>
            </li>
          <% } else { %>
              <!-- 비로그인 상태일 때, 로그인 버튼 -->
              <li class="menu-item">
                  <button onclick="window.location.href='/signin'" class="signup-link">로그인</button>
              </li>
          <% } %>
            <li class="menu-item"><button onclick="window.location.href='/mandalart'" class="nav-button active">My Mandalart</button></li>
            <li class="menu-item"><button onclick="window.location.href='/share'" class="nav-button">Share</button></li>
            <li class="menu-item"><button onclick="window.location.href='/profile'" class="nav-button">Profile</button></li>
        </ul>
    </nav>
  </header>
  <main class="flex-fill">
    <div id="container">
      <div class="form-container">
        <h2>CREATE</h2>
        <form id = "mandalart-form" action="/mandalart/create" method="POST">
          <input type="hidden" name="userId" value="<%= user.user_id %>">
          <label for="centergoal">목표</label>
          <input type="text" name="centergoal" id="centergoal" placeholder="목표를 입력하세요." required>
          <label for="tedolistCount">칸 개수</label>
          <input type="number" name="tedolistCount" id="tedolistCount" min="2" max="8" placeholder="만들고자하는 만다라트 개수를 입력하세요. * 2~8개 입력" required>
          <button type="submit" class="create-button">생성하기</button>
        </form>
      </div>

    </div>
  </main>
  
  
</body>
</html>
